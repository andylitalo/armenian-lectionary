venv/
__pycache__/
*.pyc
.DS_Store
.env
server.log
# Python packaging build artifacts
dist/
build/
*.egg-info/
# Apple Numbers spreadsheets (source-of-truth CSVs are committed instead)
*.numbers
# Dev ground-truth cache + logs (the distilled table lectionary_data.json is committed)
dev/reference_data/
# Dev Armenian-name scrape cache (the distilled *_names_hy.json maps are committed)
dev/reference_data_hy/
dev/*.log
# Regenerated on demand by dev/observance_discrepancy_report.py; it now reports no
# discrepancy at all, so a committed copy is an empty artifact that goes stale.
reports/observance_name_discrepancies.md
# Derived from dev/observance_name_review.tsv by dev/observance_name_review_atomic.py, on demand.
# A committed copy goes stale the moment the review table is edited.
dev/observance_name_review_atomic.tsv
